{% extends 'baseB.html.twig' %}

{% block body %}
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Modifier Participation</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        {#                        <li class="breadcrumb-item"><a href="{{ path('admin') }}">Home</a></li>#}
                        {#                        <li class="breadcrumb-item "><a href="{{ path('user_list') }}">Utilisateurs</a> </li>#}
                        {#                        <li class="breadcrumb-item active">Ajouter utilisateur</li>#}
                    </ol>
                </div>
            </div>
        </div>
    </section>
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-outline card-info">
                    <div class="card-header">
                        <br>
                        <div class="card-body pad">
                            {% form_theme form 'bootstrap_4_layout.html.twig' %}
                            {% form_theme form 'bootstrap_4_layout.html.twig' %}
                            {{ form_start(form) }}
                            {{ form_errors(form) }}
                            <div class="form-group">
                                {{ form_label(form.idComp) }}
                                {{ form_widget(form.idComp) }}
                            </div>
                            <div class="form-group">
                                {{ form_label(form.idu) }}
                                {{ form_widget(form.idu) }}
                            </div>
                            <div class="form-group">
                                {{ form_label(form.description) }}
                                {{ form_widget(form.drescription) }}
                            </div>
                            <div class="form-group">
                                {{ form_label(form.idp) }}
                                {{ form_widget(form.idp) }}
                            </div>

                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}